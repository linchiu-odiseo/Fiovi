

# API_BASE_URL=https://api.yangpimpollo.com


API_BASE_URL=http://localhost:2001

TENANT_SLUG=vonex
DRAFT_ENABLED=true


APP_VERSION=1.1.5

DEV_TOOLS=true

# npm run dev 
# npm run build-env  
# npm run build
# npx http-server dist/lugia/browser -p 8081 
# npx serve -s dist/lugia/browser -l 8081  


# npm run build 
# docker compose up -d --build

# 1. Clonar repo
# 2. Copiar .env.example a .env y llenar los valores
# 3. Levantar
# docker compose up -d --build
# Habilita herramientas de dev (ej. boton dado en la cartilla, atajos en /home).
# true en desarrollo, false en produccion. Cualquier valor distinto de true
# (case-insensitive) queda en false.

